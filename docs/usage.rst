Usage
=====

You need to add `tri.query` to installed apps or copy the templates to your own template directory.

Other than that it should just be to `from tri.query import Table, Column` and off you go.
